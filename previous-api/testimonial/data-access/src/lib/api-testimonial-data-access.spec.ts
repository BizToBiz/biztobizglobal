import { apiTestimonialDataAccess } from './api-testimonial-data-access'

describe('apiTestimonialDataAccess', () => {
  it('should work', () => {
    expect(apiTestimonialDataAccess()).toEqual('api-testimonial-data-access')
  })
})
