import { apiAttendanceReminderFeature } from './api-attendance-reminder-feature'

describe('apiAttendanceReminderFeature', () => {
  it('should work', () => {
    expect(apiAttendanceReminderFeature()).toEqual('api-attendance-reminder-feature')
  })
})
