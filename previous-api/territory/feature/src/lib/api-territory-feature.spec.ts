import { apiTerritoryFeature } from './api-territory-feature'

describe('apiTerritoryFeature', () => {
  it('should work', () => {
    expect(apiTerritoryFeature()).toEqual('api-territory-feature')
  })
})
