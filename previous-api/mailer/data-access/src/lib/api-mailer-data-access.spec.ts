import { apiMailerDataAccess } from './api-mailer-data-access'

describe('apiMailerDataAccess', () => {
  it('should work', () => {
    expect(apiMailerDataAccess()).toEqual('api-mailer-data-access')
  })
})
