import { apiPowerHourDataAccess } from './api-power-hour-data-access'

describe('apiPowerHourDataAccess', () => {
  it('should work', () => {
    expect(apiPowerHourDataAccess()).toEqual('api-power-hour-data-access')
  })
})
