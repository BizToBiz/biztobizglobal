import { apiPowerHourFeature } from './api-power-hour-feature'

describe('apiPowerHourFeature', () => {
  it('should work', () => {
    expect(apiPowerHourFeature()).toEqual('api-power-hour-feature')
  })
})
