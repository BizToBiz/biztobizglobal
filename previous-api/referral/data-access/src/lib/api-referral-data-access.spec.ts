import { apiReferralDataAccess } from './api-referral-data-access'

describe('apiReferralDataAccess', () => {
  it('should work', () => {
    expect(apiReferralDataAccess()).toEqual('api-referral-data-access')
  })
})
