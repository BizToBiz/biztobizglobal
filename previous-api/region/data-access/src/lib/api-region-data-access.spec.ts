import { apiRegionDataAccess } from './api-region-data-access'

describe('apiRegionDataAccess', () => {
  it('should work', () => {
    expect(apiRegionDataAccess()).toEqual('api-region-data-access')
  })
})
