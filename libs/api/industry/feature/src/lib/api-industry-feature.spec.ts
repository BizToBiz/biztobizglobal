import { apiIndustryFeature } from './api-industry-feature'

describe('apiIndustryFeature', () => {
  it('should work', () => {
    expect(apiIndustryFeature()).toEqual('api-industry-feature')
  })
})
