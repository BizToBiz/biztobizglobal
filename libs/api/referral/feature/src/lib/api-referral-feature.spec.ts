import { apiReferralFeature } from './api-referral-feature'

describe('apiReferralFeature', () => {
  it('should work', () => {
    expect(apiReferralFeature()).toEqual('api-referral-feature')
  })
})
