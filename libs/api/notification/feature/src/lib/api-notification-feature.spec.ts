import { apiNotificationFeature } from './api-notification-feature'

describe('apiNotificationFeature', () => {
  it('should work', () => {
    expect(apiNotificationFeature()).toEqual('api-notification-feature')
  })
})
