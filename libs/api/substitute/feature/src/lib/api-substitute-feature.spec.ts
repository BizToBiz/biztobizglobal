import { apiSubstituteFeature } from './api-substitute-feature'

describe('apiSubstituteFeature', () => {
  it('should work', () => {
    expect(apiSubstituteFeature()).toEqual('api-substitute-feature')
  })
})
