import { apiChapterDataAccess } from './api-chapter-data-access'

describe('apiChapterDataAccess', () => {
  it('should work', () => {
    expect(apiChapterDataAccess()).toEqual('api-chapter-data-access')
  })
})
