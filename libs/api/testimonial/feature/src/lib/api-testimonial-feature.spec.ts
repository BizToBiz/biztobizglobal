import { apiTestimonialFeature } from './api-testimonial-feature'

describe('apiTestimonialFeature', () => {
  it('should work', () => {
    expect(apiTestimonialFeature()).toEqual('api-testimonial-feature')
  })
})
