import { apiTransactionFeature } from './api-transaction-feature'

describe('apiTransactionFeature', () => {
  it('should work', () => {
    expect(apiTransactionFeature()).toEqual('api-transaction-feature')
  })
})
