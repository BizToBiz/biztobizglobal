import { apiTransactionDataAccess } from './api-transaction-data-access'

describe('apiTransactionDataAccess', () => {
  it('should work', () => {
    expect(apiTransactionDataAccess()).toEqual('api-transaction-data-access')
  })
})
